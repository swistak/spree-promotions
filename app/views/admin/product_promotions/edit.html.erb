<% form_for(@object,
            :url => object_url,
            :html => { :id => 'promotions-form', :method => :put, :multipart => true }
   ) do |f| %>
  <%= render :partial => @object.class.name.underscore, :locals => {:f => f} %>
  <%= button t("update") %>
<% end %>


