<% form_for(@object,
            :url => object_url,
            :html => { :id => 'promotions-form', :method => :put, :multipart => true }
   ) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= button t("update") %>
<% end %>


